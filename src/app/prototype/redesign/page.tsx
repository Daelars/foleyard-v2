// One full-workspace redesign: studio-console layout, ink/amber palette, icon rail,
// command bar, inspector panel, transport console. Throwaway /prototype/redesign route.
import { Workspace } from "./workspace";

export default function RedesignPrototypePage() {
  return <Workspace />;
}
