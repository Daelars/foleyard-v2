# Extension v2 implementation log

## Baseline

- Starting revision: `f7e7edd` (branch `main`), plus a large dirty working tree (staged, unstaged, and untracked docs/prototype files). Preserved; no reset performed.
- v1 system: `YARD_EXTENSION_API_VERSION = 1`, six bundled tools under `packages/yard-tools/*` (18 commands), host at `packages/yard-core/src/extensions/`, app wiring under `src/lib/extensions/` and `src/app/api/extensions/`.
- Expected-failures ledger: 12 entries in `docs/expected-failures.md`; coverage baseline 37.77% statements (docs/development.md). Full test/lint/typecheck baseline runs are deferred to #174; focused checks run per ticket.

## Decisions

- Local `.scratch` vs GitHub: user overrode the prompt's no-external-issues rule and chose GitHub issues (#164-#174).
- Ticket shape: 11 tracer-bullet tickets, blocking edges in each issue body, `ready-for-agent` label.
- ADRs written in #164: coexistence, dependency direction, permissions/trust, jobs/recovery/state versions.

## Completed
- #164: baseline recorded; 4 ADRs; traceability checklist (`docs/plans/extension-v2-traceability.md`).
- #165 (R1): framework-free v2 contracts + registry + serializable catalog under `packages/yard-core/src/extensions-v2/` (`version.ts`, `definition.ts`, `diagnostics.ts`, `registry.ts`, `catalog.ts`, `fixture-definition.ts`, `index.ts`; re-exported from `packages/yard-core/src/index.ts`). `V2_EXTENSION_API_VERSION = 2`; registry validates duplicate IDs, namespace ownership, malformed defaults/options, unsupported API versions, unresolved command refs, unknown permissions, unsupported contribution/scope types, malformed schemas; `assertJsonSerializable` throws on function/symbol/bigint/undefined leakage with JSON paths. Fixture `fixture-greeter` registers through the single `register()` path with no host branches.
- #166 (R2): canonical invocation/outcome contracts, shared availability evaluator, and single-path transport under `packages/yard-core/src/extensions-v2/` (`invocation.ts`, `selection.ts`, `availability.ts`, `host.ts`, `transport.ts`, all re-exported via `index.ts`). `vinv_` invocation IDs; ownership resolved before hydration; availability (enabled, scope context, selection, input, unknown-capability denial, granted permissions) is one pure function used by renderer reads and execution preflight; selection boundary rejects client file paths and resolves IDs via narrow `V2LibraryPorts`; handlers register ownership-keyed (scope-contract resolvers, no command-name tables); v2 routes `/api/extensions-v2`, `/api/extensions-v2/availability`, `/api/extensions-v2/execute` with documented status map (200 immediate/review, 202 job, 400 invalid, 403 disabled/denied, 404 unknown/unresolvable, 413 over limits, 500 host faults), limits (256 KiB body, 64 KiB input, 500 IDs), and `{ ok, error }` envelopes. R1 fix found by R2 tests: `validateV2Value` dropped the nested value for object properties (validated the path string instead); now passes `record[name]` — existing R1 tests still pass. Thin Next route wrappers deferred to #167/#173 (permission approval policy is #167's).
- #167 (R3): deny-by-default permissions plus narrow operation services. Core additions under `packages/yard-core/src/extensions-v2/` (`permissions.ts`, `grants.ts`, `filesystem.ts`, `operations.ts`, re-exported via `index.ts`): `computeEffectiveV2Permissions` (declared ∩ approved, declaration order, no inference/auto-grant) shared by the handler context and `buildEffectiveV2Catalog`; `V2PermissionApprovals` explicit in-memory policy with expiry (persistence deferred to R7 state work); `V2GrantStore` for destination grants (missing/expired/foreign denied, no token material in records); pure filesystem guards (`authorizeV2ReadablePath`/`authorizeV2WritablePath`: lexical traversal screen, canonical-path containment, link-escape and dangling-link denial, existing-ancestor resolution) over injected `V2PathIo`, with the validation-to-use race documented honestly in the module docblock; `createV2OperationServices` narrow surface (paged Library reads, named selection sources, authorized file read/copy/output creation, ZIP archive behind `V2ArchivePorts`, namespaced settings/state, job reporter, job-owned workspace with owned-only `dispose`) with an explicit per-method permission map and `V2OperationError` carrying typed failure codes. `host.ts` builds per-invocation services through an app-supplied `createOperations` factory (deny-all default), computes effective permissions via the shared function, and maps operation errors to failures. App side (new files only): `src/lib/extensions-v2/` (`policy.ts`, `library-ports.ts` over repository contracts, `filesystem.ts` node ports plus picker-token→grant-ID bridge reusing `filesystem-boundary.ts`, `settings-state.ts` over database infrastructure, `archive.ts` contract stub with codec wiring deferred to #171, `host.ts` composing the app host) and thin wrappers `src/app/api/extensions-v2/` (catalog, availability, execute). v1 endpoints/adapters/packages untouched. Deferred to later tickets: approval persistence (R7/#169), real ZIP bytes (#171), named shelf/recent source registration (#171), job lifecycle/cancellation semantics (R4/#168).

- #168 (R4): host-owned job lifecycle with polling transport. Core additions under `packages/yard-core/src/extensions-v2/` (`jobs.ts` new, re-exported via `index.ts`): `V2JobManager` with queued/running/cancellation-requested/succeeded/failed/cancelled/interrupted states, `vjob_` IDs, FIFO start under a max-2 concurrency bound, created/started/finished plus cancellation-requested-vs-stopped timestamps, reporter-driven progress, explicit partial outcomes (succeeded count, first-50 failures, incomplete flag/reason), cooperative cancellation (`V2JobCancelledError`; terminal states only after the runner settles, so never stopped while still writing; late-finishing runners stand with the request time kept), idempotency keys (same extension+command+key returns the existing job; scope validation; keys forgotten only when bounded 100-record history evicts), snapshot/restore (serializable records with grant-ID/root ownership metadata, handler values and grant tokens never persisted; restore marks live jobs interrupted with known outputs and a recovery note, never replays), disable gates (submit-time and at-queue-front denial plus `cancelExtensionJobs`: queued→cancelled, running→cancellation-requested). `host.ts` refactored to a shared preflight used by both `execute` (unchanged behavior) and new `submitJob` (same preflight, then the registered handler runs with a job-bound reporter; immediate returns validate into completion values, `review-required`/`job` returns fail `result-invalid`, operation errors map to typed codes); `createOperations` binding gains an optional reporter so per-call file/archive cancellation checks observe the job signal; injectable shared `jobManager`. `transport.ts`: stateless polling codec (`POST /api/extensions-v2/jobs` 202 submit with duplicate flag, `GET` list/status, `POST` cancel; idempotent cancel; `job-unknown` 404; idempotency-key envelope validation); `invocation.ts` gains the key/outcome fields. App side (new files only): `src/lib/extensions-v2/jobs.ts` (process-wide manager, per-job operation wiring, snapshot persistence as one row in the existing `settings` table — no migration — with once-per-boot restore, persistence failures never fail transitions) and `job-client.ts` (poll-until-settled with backoff, reload-by-ID, cancel), `host.ts` (singleton `getAppV2Host` sharing one manager, reporter forwarding, disable requests cancellation of live jobs), routes `src/app/api/extensions-v2/jobs/` (submit/list, status, cancel). Grants already expire on restart (in-memory store); named selection sources and real ZIP bytes stay deferred to #171. Six v1 packages plus v1 routes/adapters untouched (only pre-existing dirty-tree entries remain); no commits made.

- #169 (R5+R7): host-validated prepare/review/apply plus settings, state, events. Core additions under `packages/yard-core/src/extensions-v2/` (re-exported via `index.ts`, no v1 imports per boundary test): `plans.ts` new (`V2PlanManager` with `vplan_` IDs binding extension/command/validated targets+options/auth context/expiry/invocation, canonical-hash echo check, 15-min default TTL capped at 1h, single-use consumption, destructive review gate, generic `V2PlanPreview` tables/notices/serializable details + reversibility declaration as the Make Pack channel); `extension-data.ts` new (`V2AuthoredSettingsStore` declaration-validated get/set/reset with corrupt-load fallback + diagnosis, `V2WorkflowStateStore` versioned envelopes with transactional `migrateTo`, legacy-blob tolerance, 128-key/64KiB bounds, disable-with-diagnosis); `events.ts` new (`V2EventBus` typed settings/state/approvals/job-transition/contributions events, sequence numbers, unsubscribe disposal, host-only emit). Extended: `invocation.ts` (+5 failure codes), `definition.ts` (`validateV2SettingValue`), `permissions.ts` (approval snapshot/restore), `jobs.ts` (snapshot retention: newest 50 of 100-record history), `operations.ts` (`plans.prepare` bound to the host manager + optional settings-declaration validation + `settings.reset`), `host.ts` (owned `plans` manager, execute-time plan-binding check returning stored summary/expiry, `reviewPlan` stamp, `applyPlan` with target/grant/permission/availability revalidation, consume-on-execution-start), `transport.ts` (statuses + plan review/apply codec + routes). App side (new files only): `src/lib/extensions-v2/events.ts` (process bus), `settings-state.ts` (settings-table rows, persist-before-notify emission, store factories), `policy.ts` (approval persistence under `v2:approvals`, loaded once at boot), `jobs.ts` (transition-only emission; progress stays memory-only), `host.ts` (declaration lookup + grant re-auth hook), `src/app/api/extensions-v2/plans/[planId]/` review + apply routes. Destructive behavior exercised by a `fixture-pack` fixture over fake files (copies to a grant dir, unrelated files survive); no destructive end-user extension, no generic undo (reversibility kinds documented). v1 routes/adapters/packages untouched.

- #170 (R6): generic adapters for all 8 contribution points. Core `packages/yard-core/src/extensions-v2/contributions.ts` new (re-exported via `index.ts`, no v1 imports per boundary test): `contributionPointForType` (file/folder context menus share one adapter), `resolveV2PointContributions` (stable `v2:{ext}:{contrib}` keys, `order ?? 100` ascending with extension/contribution tiebreak, same-command-twice keeps both entries, disabled filtered, unknown commands skipped, availability attached via the shared evaluator), `sanitizeV2SelectionIds`, `inputFieldsForSchema` + defaults (interaction point is schema/review/job data, not a registered type), `validateV2DropCandidates` (count/names/bound; audio screening and capability checks stay in the app adapter/evaluator). R1 fix found by R6 fixtures: `toCatalogEntry` spread explicit `undefined` for undeclared setting `options` and contribution `title`/`order`/`description`, which its own `assertJsonSerializable` rejects — every non-enum setting failed projection (the greeter fixture only has an enum setting, so R1 tests never caught it); undeclared optionals now stay absent via `withoutUndefined`, and existing catalog/registry tests still pass. App side (new files only): `src/lib/extensions-v2/contributions.ts` (`resolveV2UiPoint`, catalog/enabled clients, `invokeV2Command`, `useV2Catalog` with abort + visibility refresh + idempotent dispose, `useV2UiPoint`), `fixtures.ts` (dev-only surface fixture covering palette/file/folder/selection/toolbar/sidebar/settings/drop + worker fixture for jobs/permissions/state; `registerV2DevFixtures` throws in production, registers once, records explicit approvals; never enters production catalogs), `host.ts` emits `contributions-changed` on register/enable-toggle/unregister via `registerV2Extension`/`unregisterV2Extension` (registration never enables). Routes: `extensions/` GET states, `extensions/[extensionId]` PATCH enable/disable, `settings/[extensionId]` GET declarations+values+permissions, `settings/[extensionId]/[settingId]` PUT validated write + POST reset, catalog GET dev-fixture gate behind `FOLEYARD_V2_DEV_FIXTURES=1`. Generic renderers `src/components/extensions-v2/` (shared focus-restore/reason, palette roving listbox section, menus incl. context/dropdown items + selection actions + toolbar + real `V2DropMenu` over genuine drop events with audio screening, sidebar loading/error/retry/empty/items, settings validated controls + defaults + reset + enable/disable + permission badges, interaction schema fields + plan preview tables/notices/details + job progress via `fetchV2JobStatus`/`pollV2JobUntilSettled` + result details), plus `use-v2-palette` bridge (`v2tool:` entries with toasts) and `V2LibraryDropZone` wrapping the real FileTable region. Additive real-app wiring only: palette builder + `v2tool` dispatch, FileRowMenu `v2Items`, bulk-bar `v2Actions` slot, ExtensionsTab `v2Settings` slot, page drop zone; v1 entries/shortcuts/routes/packages untouched. A11y: roles, aria-disabled with reasons, focus restoration, Escape dismiss, `motion-reduce:` utilities, theme tokens, wrapping narrow layouts.

## Test evidence

- #164: documentation-only; no code changed. Verification: `gh issue list` shows #164-#174 open with `ready-for-agent`.
- #165: `npx vitest run packages/yard-core/src/extensions-v2 src/test/integration/yard-core-services.test.ts src/test/integration/yard-core-filename.test.ts src/test/integration/extension-host-transport.test.ts --no-coverage` → 5 files, 50 passed + 1 pre-existing expected fail. `bunx tsc --noEmit` → clean (`npx tsc` shim fails in this env before compiling; bunx used instead). New tests: 22 (registration validation ×13, catalog leakage/round-trip/parse ×5, schema runtime validation ×1, v1-import boundary ×1, clone isolation ×1, fixture entry ×1). Six v1 packages under `packages/yard-tools/*` untouched (only pre-existing dirty-tree entries remain).
- #166: `bunx vitest run packages/yard-core/src/extensions-v2 src/test/integration/yard-core-services.test.ts src/test/integration/yard-core-filename.test.ts src/test/integration/extension-host-transport.test.ts --no-coverage` → 10 files, 119 passed + 1 pre-existing expected fail. `bunx tsc --noEmit` → clean. `bunx eslint packages/yard-core/src/extensions-v2` → 0 errors (3 pre-existing `entryId` warnings in R1 `registry.ts`). New tests: 69 (`invocation` 6, `availability` 9, `selection` 12, `host` 16, `transport` 26 — agreement matrix, unknown caps, invalid contexts, ownership-before-hydration spy, limits, envelopes). v1 routes/adapters untouched; no commits made.
- #167: `bunx vitest run packages/yard-core/src/extensions-v2 src/test/integration/yard-core-services.test.ts src/test/integration/yard-core-filename.test.ts src/test/integration/extension-host-transport.test.ts --no-coverage` → 15 files, 151 passed + 1 pre-existing expected fail. `bunx tsc --noEmit` → clean. `bunx eslint packages/yard-core/src/extensions-v2 src/lib/extensions-v2 src/app/api/extensions-v2` → 0 errors (same 3 pre-existing `entryId` warnings). New tests: 50 across 5 files (`permissions` 10, `grants` 5, `filesystem` 11, `operations` 21, `boundaries` 3 — intersection order, default-deny, expiry, catalog parity, secret scan; missing/expired/foreign grants; traversal, containment, symlink/junction escapes, dangling links, existing-ancestor resolution; unchecked-handler confinement incl. host mapping to `permission-denied`, read enforcement, grant denial, no-overwrite conflicts, owned-only cleanup, bounded 5,200-record paging, named sources, settings namespace, state isolation, archive manifest/collision/limit rules; import-specifier scan, empty runtime deps). Shared fakes moved to `test-helpers.ts` so test files no longer import each other's suites. Six v1 packages under `packages/yard-tools/*` plus v1 routes/adapters untouched (only pre-existing dirty-tree entries remain); no commits made.

- #168: `bunx vitest run packages/yard-core/src/extensions-v2 src/test/integration/yard-core-services.test.ts src/test/integration/yard-core-filename.test.ts src/test/integration/extension-host-transport.test.ts --no-coverage` → 16 files, 178 passed + 1 pre-existing expected fail. `bunx tsc --noEmit` → clean. `bunx eslint packages/yard-core/src/extensions-v2 src/lib/extensions-v2 src/app/api/extensions-v2` → 0 errors (same 3 pre-existing `entryId` warnings). New tests: 27 in `jobs.test.ts` (lifecycle/IDs/timestamps, FIFO bound, progress, partial/incomplete, typed failure codes with no v1 fallback, cancel-hold-until-settled with request-vs-stop ordering, queued cancel, late-finish audit, operations-level cancel between ops, duplicate invocation keys incl. terminal dedup, key scope/shape, history-eviction forgetting, restart interruption without replay, malformed restore entries, token-free snapshots via secret scan, disable rejects/cancels, owned-only disposal with unrelated files surviving, 5,200-record bounded paging over operation services in 11 pages, incomplete probe report, shared submit preflight, duplicate submit flag, job-mode result validation, HTTP submit/status/list/cancel incl. 404/400 paths). Six v1 packages under `packages/yard-tools/*` plus v1 routes/adapters untouched; no commits made.

- #169: `bunx vitest run packages/yard-core/src/extensions-v2 src/test/integration/yard-core-services.test.ts src/test/integration/yard-core-filename.test.ts src/test/integration/extension-host-transport.test.ts --no-coverage` → 19 files, 205 passed + 1 pre-existing expected fail. `bunx tsc --noEmit` → clean. `bunx eslint packages/yard-core/src/extensions-v2 src/lib/extensions-v2 src/app/api/extensions-v2` → 0 errors (same 3 pre-existing `entryId` warnings). New tests: 28 (`plans` 13: full prepare/review/apply flow with stored-record binding, forged-plan rejection, altered echo with pending retry, replay-as-consumed, TTL expiry, unknown plans, destructive review gate over fake files with unrelated-file survival, revoked-grant denial, removed-target rejection, lapsed-permission denial, Make Pack preview channel round-trip, plan HTTP codec + status map; `extension-data` 8: defaults/validation/reset, corrupt-load diagnosis, persist-before-notify ×2, state isolation, legacy-blob tolerance, transactional migration with failure preservation + disable/reset recovery, bounds; `events` 4: typed delivery, wildcard/disposal, listener counts, gap sequence; `permissions` +1 approval snapshot/restore; `jobs` +1 snapshot retention of newest 50; `host` review test updated for binding enforcement). Six v1 packages under `packages/yard-tools/*` plus v1 routes/adapters untouched; no commits made.

- #170: `bunx vitest run packages/yard-core/src/extensions-v2 src/components/extensions-v2 src/test/integration/extensions-v2-ui.test.tsx src/test/integration/yard-core-services.test.ts src/test/integration/yard-core-filename.test.ts src/test/integration/extension-host-transport.test.ts --no-coverage` → 22 files, 235 passed + 1 pre-existing expected fail. `bunx tsc --noEmit` → clean. `bunx eslint` over v2 + touched files → 0 errors (same 3 pre-existing `entryId` warnings). New tests: 30 (`contributions` 10: point mapping, stable keys + reasons, ordering/tiebreak, keep-both collisions, disabled/unknown exclusion, capability denial, selection sanitization, form fields, drop validation, catalog `undefined`-optional regression; `adapters` 13 jsdom interaction, one per point incl. context changes + disable cleanup; `extensions-v2-ui` 7: real FileRowMenu/CommandPalette/ExtensionsTab/SelectionBulkBar integration + PATCH enable/disable with `contributions-changed` emission and UI removal + settings GET/PUT-validation/POST-reset + 404/400 paths). Regression guard: `workspace-smoke` + `client-mutation-lifecycle` (cover the edited palette/menu/bar/page files) → 5 passed + 3 pre-existing expected fails. Six v1 packages under `packages/yard-tools/*` plus v1 routes/adapters untouched (only pre-existing dirty-tree entries remain); no commits made.

- #171 (R8): `bunx vitest run packages/yard-core/src/extensions-v2 packages/yard-tools/make-pack-v2 src/components/extensions-v2 src/components/extensions/make-pack-v2 src/test/integration/make-pack-v2.test.ts src/test/integration/extensions-v2-ui.test.tsx src/test/integration/yard-core-services.test.ts src/test/integration/yard-core-filename.test.ts src/test/integration/extension-host-transport.test.ts --no-coverage` → 26 files, 284 passed + 1 pre-existing expected fail. `bunx tsc --noEmit` → clean. `bunx eslint` over v2 + touched files → 0 errors (same 3 pre-existing `entryId` warnings). New package `packages/yard-tools/make-pack-v2/` (definition with 3 commands/3 namespaced settings/7 contributions, pure `policy.ts`, `handlers.ts` on v2 services only, README parity table); general engine gaps solved in core, not as Make Pack branches: `V2HandlerContext.runMode` (`direct` previews without side effects, `apply`/`job` export) in `host.ts` + `V2RunMode` export, manifest forwarded as an in-memory `V2ArchiveEntry` in `operations.ts` (fixes the v1 B12 class: no predictable dot-tmp sidecar, nothing to delete in `finally`). App `src/lib/extensions-v2/` (real stored-ZIP streaming `archive.ts` codec with ZIP32 limits + partial-output removal, `sources.ts` shelf/recent adapters over persisted records with unavailable-store errors instead of silent empty, `make-pack-v2.ts` registration wired at the bottom of `host.ts` with `selectionSources`, `grants` + `approvals` routes); UI `MakePackV2Dialog` + hook (preview → destination grant → cancellable job → capability-aware reveal), `V2ExtensionsSection` with explicit Approve, `V2ExtensionSettings` approve button, page prop-drilling (`resolveV2FileItems`/`onV2Command` through the FileTable chain, bulk-bar `v2Actions`, `v2Settings` through SettingsDialog types/body/tab, shelf/bulk Pack-v2 entries routing make-pack-v2 commands to the dialog). New tests: 49 (`policy` 11; `handlers` 16 incl. contributions resolution + no-v1-import scan; app integration 17 incl. real-fs exports for all 3 sources × folder/ZIP, independent EOCD/central-directory ZIP-reader integrity, interruption, 501-entry bound, sidecar preservation, registration/routes/boundary; dialog 3 jsdom; core `host` runMode + `operations` manifest-entry). Regression guard: `workspace-smoke` + `client-mutation-lifecycle` + `data-loss-prevention` + `filesystem-boundary` → 4 files, 16 passed + 8 pre-existing expected fails (v1 B12 stays expected-fail; v1 behavior unchanged). Full suite `bunx vitest run --no-coverage` → 41 files, 356 passed + 12 pre-existing expected fails, no regressions. Six v1 packages under `packages/yard-tools/*` plus v1 routes/adapters untouched (only pre-existing dirty-tree entries remain, plus `bun.lock` from `bun install` linking the new workspace); no commits made.

## Completed (appended for #172)

- #172 (R9): new files only. `scripts/scaffold-extension-v2.cjs` (scaffold CLI with `--name/--out/--title/--description/--force`, kebab-case validation, writes `package.json` + `src/definition.ts` + `src/handlers.ts` + `src/index.ts` + `src/handlers.test.ts` + `README.md` carrying the explicit static-registration snippet; refuses occupied outputs without `--force`). `src/app/prototype/ext-v2-workbench/` (`page.tsx` client workbench reusing the production `resolveV2UiPoint`/`fetchV2JobStatus`/`requestV2JobCancel` clients plus shadcn Button/Input and `v2PanelClass`: sanitized catalog, 9-point contributions preview, availability check, direct/job invoke with `runMode` echo, cancel, host-history panels; `inspector.tsx` bounded execution-inspector log with `runMode` badges, transitions, denied permissions, local detail views, and sanitized-JSON export; `redact.ts` shared redactor preserving `vinv_/vjob_/vplan_` IDs while redacting paths/settings-values/tokens/secrets/stacks within 2000-char and 100-record bounds; `fixture-handlers.ts` dev-only surface+worker handler registration on the app host, idempotent via `hasHandler`; `reload/route.ts` explicit reload returning per-call attached handlers; `history/route.ts` sanitized bounded job history). Tests: `src/test/integration/extension-v2-scaffold.test.ts` + `extension-v2-workbench.test.ts` plus colocated `redact/inspector.test.ts`. Fixture design: `fixture-surface` stays pure/read-only over the otherwise-unused points (file/folder context menus, collection/drop scopes, toolbar/sidebar/settings entries) with per-command `runMode=` echoes; `fixture-worker.count-library` pages `operations.library` with reporter progress and `throwIfCancelled`, always probes the deliberately undeclared `files:write` (caught denial surfaces as `denied=files:write`), and writes isolated `last-count` state only its own namespace can read. No fixture-ID branches in production code (scan test over api/lib/components/core/make-pack-v2, allowlisting only the two definition sources). Production exclusion is three-layer (prototype layout `notFound` + route-level 404 + electron-builder `!.next/server/app/prototype/**`) with `registerV2DevFixtures` throwing in production. Constraint call: handler registration lives in the dev-route-colocated `fixture-handlers.ts`, not `src/lib`, so no existing file was modified; `package.json` gained no script entry — invocation is `node scripts/scaffold-extension-v2.cjs --name <slug>` as printed by the script and its README output. Six v1 packages under `packages/yard-tools/*` plus v1 routes/adapters untouched; no commits made.

## Test evidence (appended for #172)

- #172: focused `bunx vitest run packages/yard-core/src/extensions-v2 packages/yard-tools/make-pack-v2 src/components/extensions-v2 src/app/prototype/ext-v2-workbench src/test/integration/extensions-v2-ui.test.tsx src/test/integration/make-pack-v2.test.ts src/test/integration/extension-v2-scaffold.test.ts src/test/integration/extension-v2-workbench.test.ts src/test/integration/yard-core-services.test.ts src/test/integration/yard-core-filename.test.ts src/test/integration/extension-host-transport.test.ts --no-coverage` → 28 files, 300 passed + 1 pre-existing expected fail (delta over #171: 19 new tests — `redact` 7, `inspector` 2, scaffold 4 incl. a scoped `tsc -p` typecheck and a child `vitest run` of the generated suite, workbench 8 incl. fixture execution with `runMode` echoes, job progress with a named denial, state isolation, reload idempotency/disposal, production refusal, no-branch scan, and build-exclusion proof; the `dialog` 3 from #171 were out of this run's scope). `bunx tsc --noEmit` → clean. `bunx eslint` over the new files → 0 errors (one `set-state-in-effect` hit during development, fixed by restructuring the mount fetch into a cancellable async closure instead of calling the refresh callback). Probe scaffold output verified end to end: `node scripts/scaffold-extension-v2.cjs --name scaffold-probe` → 3/3 generated tests pass, scoped typecheck clean, probe removed afterwards (no tree dirt).

## Completed (appended for #173)

- #173 (R10): runtime discovery, docs, diagrams, build/CI. `src/lib/runtime-info.ts` gains `extensionSystems` (v1 API 1 + v2 API 2 with registered IDs), per-extension `extensionsV2` (identity, in-memory enablement, declared vs effective permissions with `approvalsKnown`, command/contribution IDs, settings schema with defaults only, docs refs), and `eventsV2` (the five typed contracts). New `readRuntimeDatabaseFlags` helper reads `extension:*:enabled` + `v2:approvals` through a short-lived read-only handle only when the file exists, replacing the read-write `getExtensionEnabled` overlay. Four new guides registered in `DOCUMENT_REGISTRY` + both docs scripts: `docs/extensions-v2.md` (authoring/API + 9 runnable author examples), `docs/extensions-v2-migration.md`, `docs/extensions-v2-make-pack.md`, `docs/extensions-v2-troubleshooting.md`. Additive updates to 13 guides + root README + RELEASE.md. `public/extension-system-v2.html` companion (5 offline SVG diagrams: v1-vs-v2 execution, module deps, UI resolution, job states, coexistence/migration) linked from the proposal with shipped-vs-proposal status. Runnable `examples/extensions-v2/minimal/` (`bun run example:v2-minimal`, 4/4 cases) registered in the manifest. `scripts/check-v2-boundaries.cjs` (import direction + workspace inclusion + prototype exclusion) wired into `check.yml` with a package script. Contradictions found and fixed: (C1) snapshot initialized/migrated the DB via the read-write overlay despite the never-initializes claim; (C2) desktop guide said staged-docs `extraResources` was proposed-but-undeclared while `electron-builder.yml` declares it; (C3) five guides claimed no `examples/` directory while two runnable examples shipped; (C4) development guide said 12 and 14 expected-failure entries in the same file (actual 12).

## Test evidence (appended for #173)

- #173: `bunx vitest run src/test/integration/extensions-v2-discovery.test.ts src/test/integration/extension-v2-repo-contracts.test.ts --no-coverage` → 2 files, 12 passed. `node scripts/prepare-docs.cjs` → 26 docs staged; `node scripts/check-docs.cjs` → OK (26 sources, manifest identity, links, 18/18 command refs). `node scripts/check-v2-boundaries.cjs` → OK. `bun run example:v2-minimal` → 4/4 cases. Scaffold probe (`--name verify-ex1`) → generated 3/3 tests pass, probe removed. `bunx tsc --noEmit` → clean. Full-suite + lint + coverage evidence deferred to #174 per the delivery order.

## Remaining (updated for #173)

- #174 in dependency order. Frontier after #173: #174 (R11 full verification incl. production-build fixture exclusion and the non-publishing desktop package) — unblocked by this ticket.

## Completed (appended for #174)

- #174 (R11): full evidence pass, additive only (RELEASE.md enable/disable subsection + traceability/log rows; no code, threshold, or test changes). Required checks: `bun run typecheck` clean; `bun run lint` 0 errors, 3 pre-existing `entryId` warnings in `registry.ts`; `bun run test` → 47 files, 389 passed + 12 expected fails; `bun run test:coverage` → 64.29% statements / 54.59% branches / 59.57% functions / 65.56% lines vs thresholds 36.5/31/34/37.5; `node scripts/check-expected-failures.cjs` → 12 pinned, all accounted for; `node scripts/check-v2-boundaries.cjs` → OK; `bun run docs:prepare` → 26 staged, `bun run docs:check` → OK (18/18 command refs); `bun run example:selected-ids` 3/3, `bun run example:v2-minimal` 4/4, `node examples/core/query-library/run.ts` assertions pass; `bun run build` clean. v2-scoped run (core extensions-v2 + make-pack-v2 + v2 components + workbench + 6 v2 integration suites) → 29 files, 289 passed. Scaffold probe (`--name r11-probe-ext`) → generated 3/3 pass, probe removed, `packages/yard-tools/` shows no leftover. UI inspection: source-level review of `src/components/extensions-v2/` + `MakePackV2Dialog.tsx` (keyboard handlers in palette/menus/settings, `useV2FocusRestore`, `aria-disabled`+reason titles, `role=alert/status/progressbar`, `aria-invalid` validation, `v2PanelClass` theme tokens, wrap/truncate narrow behavior; jsdom interaction assertions incl. `aria-valuenow=100`) — no live browser render available in this environment, so no screenshot claim. Desktop `bun run release:build` BLOCKED: `clean-binary` refused (better_sqlite3.node locked by a running process; exact banner recorded; killing node.exe would end the agent session). Dev binary verified intact afterwards (catalog suite 7/7). No packaged install claimed; `dist-electron/` holds only stale 0.1.0 artifacts. Release-notes gap closed: RELEASE.md "Extension v2 status" gained an enable/disable subsection pointing at `docs/extensions-v2-migration.md` + `docs/extensions-v2-make-pack.md`. v1 preservation: session touched no v1 file (dirty-tree v1 diffs predate #174, from the #161 command-definitions refactor); six tools keep 18 commands, routes, settings, data. Nothing committed, pushed, or published.

## Test evidence (appended for #174)

- #174: see commands above. Per-evidence-item mapping to the existing suites (all passing in this pass): registration/ownership/compat/schemas/serializability → `registry` + `catalog` (22); unauthorized-handler confinement → `operations`/`permissions` (unchecked-handler + host mapping); direct-vs-HTTP agreement → `host` + `transport` (agreement matrix, envelopes, status map); grants/traversal/conflicts/cleanup → `grants` + `filesystem` + `operations`; 5000+ bounded iteration → `jobs` (5200-record paging, 11 pages) + `operations` bounded-5,200 test; cancellation/partial/duplicate/concurrency/disable/reconnect/restart → `jobs` (27: lifecycle, partial/incomplete, cancel-hold, duplicate keys, max-2 concurrency, disable/dispose, reconnect polling codec, restart interruption without replay) + app `job-client` reconnect; state isolation + migration safety → `extension-data` (isolation, transactional migration, failure preservation) + `permissions` snapshot/restore; plan replay policy → `plans` (13: forged/altered/expired/replayed/consumed); Make Pack real exports → `handlers` (16) + `make-pack-v2` integration (17: real-fs folder/ZIP, manifest on/off, collisions, missing, conflicts, interruption, B12 sidecar class, ZIP-reader integrity); adapter interaction → `adapters` (13 jsdom, one per point + context changes + disable cleanup) + `extensions-v2-ui` (7 real FileRowMenu/CommandPalette/ExtensionsTab/BulkBar); keyboard/focus/validation/progress/results/theme/narrow → jsdom assertions + source review (no live render; stated honestly); scaffold/examples/reload/prod-exclusion → `scaffold` (4) + `workbench` (8) + repo-contracts build-contents test + this pass's live probe; read-only introspection → `discovery` (6: read-only temp-DB proof, no-handler-execution, no-secrets); docs staging → repo-contracts (staged prepare+check round-trip) + this pass's live 26-doc staging; build contents → repo-contracts + this pass's `next build`; v1 preservation → full suite green with the 12-entry ledger unchanged.
