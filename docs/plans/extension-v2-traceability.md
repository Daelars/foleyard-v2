# Extension v2 traceability checklist

Source: `docs/plans/extension-v2-implementation-prompt.md`. Tickets: #164-#174. Updated when behavior changes; otherwise the row records why no change was needed.

| Req | Ticket | Implementation | Tests | Docs |
| --- | --- | --- | --- | --- |
| R1 definitions, discovery, compat | #165 | DONE: `packages/yard-core/src/extensions-v2/` (`version.ts` API v2=2, `definition.ts` contracts + `validateV2Value`, `diagnostics.ts`, `registry.ts` validation, `catalog.ts` serializable projection, `fixture-definition.ts` greeter fixture); no v1 imports (boundary test) | `registry.test.ts` + `catalog.test.ts` (22 tests: validation codes, leakage paths, round-trip, boundary) + tsc clean | compat rules in `version.ts` docblock; full authoring/API guide deferred to #173 |
| R2 invocation and availability | #166 | DONE: `packages/yard-core/src/extensions-v2/` (`invocation.ts` IDs/ownership/outcome union/failure codes/limits, `selection.ts` boundary parse + scope-keyed resolvers over `V2LibraryPorts`, `availability.ts` shared pure evaluator, `host.ts` single-path `ExtensionV2Host` with ownership-keyed handler table, `transport.ts` v2 routes/status map/envelopes/pure HTTP codec); v1 endpoints untouched; no v1 imports (boundary test) | `invocation/availability/selection/host/transport.test.ts` (69 new tests: direct-vs-HTTP agreement matrix, unknown caps, invalid contexts, ownership-before-hydration, preflight recheck, limits 413, envelope shape) + tsc clean | routes/statuses/limits/envelopes documented in `transport.ts` docblock; commands/transport guides deferred to #173 |
| R3 permissions and operations | #167 | DONE: `packages/yard-core/src/extensions-v2/` (`permissions.ts` declared∩approved + `V2PermissionApprovals` + effective catalog + secret scan, `grants.ts` destination-grant store, `filesystem.ts` pure ADR guards over `V2PathIo`, `operations.ts` narrow per-invocation services + deny-all default, `host.ts` factory wiring + error mapping, `test-helpers.ts`); app `src/lib/extensions-v2/` (policy, repository-backed Library ports, node file ports + picker-token bridge reusing `filesystem-boundary.ts`, settings/state over database infrastructure, archive contract with codec deferred to #171) and thin `src/app/api/extensions-v2/` wrappers (catalog, availability, execute); no v1 imports (specifier-level boundary test + empty runtime deps) | `permissions/grants/filesystem/operations/boundaries.test.ts` (50 tests: unchecked-handler confinement, expiry, traversal/junction, conflicts, owned-only cleanup, bounded paging, namespace isolation, forbidden imports) + tsc clean + eslint 0 errors | permission/trust + dependency-direction ADRs (unchanged); filesystem guide and full authoring docs deferred to #173 |
| R4 jobs, progress, cancel, failure | #168 | DONE: `packages/yard-core/src/extensions-v2/` (`jobs.ts` host-owned `V2JobManager` with queued/running/cancellation-requested/succeeded/failed/cancelled/interrupted states, `vjob_` IDs, max-2 concurrency, timestamps, progress, explicit partial outcomes, cooperative cancellation with request-vs-stop times, idempotency keys, bounded 100-record history, snapshot/restore with restart interruption; `host.ts` shared preflight + `submitJob` running registered handlers with job-bound reporters, disable gate rechecked at start; `transport.ts` stateless polling codec + `job-unknown` 404 + idempotency-key validation; `invocation.ts` key/outcome fields); app `src/lib/extensions-v2/` (`jobs.ts` process-wide manager + settings-table snapshot persistence + boot restore, `host.ts` singleton host + reporter forwarding + disable-cancels-jobs, `job-client.ts` reconnect/reload polling) and `src/app/api/extensions-v2/jobs/` routes (POST submit 202, GET list/status, POST cancel); no v1 imports (boundary test scans new modules) | `jobs.test.ts` (27 tests: lifecycle, FIFO bound, progress, partial/incomplete, cancellation hold-until-settled, queued cancel, late-finish audit, operations-level cancel, duplicate keys, key scope/shape, eviction, restart interruption without replay, malformed restore, token-free snapshots, disable/dispose, 5200-record bounded paging, submit preflight sharing, transport polling) + tsc clean + eslint 0 errors | limits and recovery documented in `jobs.ts` docblock; full troubleshooting guide deferred to #173 |
| R5 preview, review, apply | #169 | DONE: `packages/yard-core/src/extensions-v2/` (`plans.ts` host-owned `V2PlanManager` binding extension/command/validated targets+options/auth context/expiry/invocation with canonical-hash echo, TTL, single-use, destructive review gate, generic `V2PlanPreview` tables/notices/details; `host.ts` binding check + `reviewPlan`/`applyPlan` with target/grant/permission/availability revalidation; `operations.ts` `plans.prepare`; `transport.ts` review/apply codec + statuses; `invocation.ts` +5 codes); app `src/lib/extensions-v2/host.ts` (grant re-auth hook) + `src/app/api/extensions-v2/plans/` routes | `plans.test.ts` (13: binding, forged/altered/expired/replayed rejection, pending retry, destructive gate over fake files, grant/target/permission revalidation, Make Pack channel, codec) + updated `host.test.ts` review binding | policy/retry/undo rules in `plans.ts` docblock; authoring examples (form, preview, reviewed action) deferred to #172/#173 |
| R6 contribution points | #170 | DONE: core `packages/yard-core/src/extensions-v2/contributions.ts` (point mapping incl. file/folder→context-menu, stable `v2:{ext}:{contrib}` keys, `order ?? 100` + ext/contrib tiebreak, keep-both collision rule, availability attached per item, `inputFieldsForSchema` form fields, `validateV2DropCandidates`); R1 fix: `toCatalogEntry` no longer emits explicit `undefined` for undeclared setting `options`/contribution `title`/`order`/`description` (rejected by its own serializer; broke every non-enum setting). App `src/lib/extensions-v2/` (`contributions.ts` UI resolver + catalog/enabled clients + `invokeV2Command` + `useV2Catalog`/`useV2UiPoint` with abort/dispose, `fixtures.ts` dev-only surface+worker fixtures registering through the production registry, gated by `FOLEYARD_V2_DEV_FIXTURES=1` + non-production); `host.ts` emits `contributions-changed` on register/enable-toggle/unregister. Routes: `extensions/` GET states + `[extensionId]` PATCH enable/disable, `settings/[extensionId]` GET + `[settingId]` PUT validated write / POST reset, catalog GET dev-fixture gate. Generic renderers `src/components/extensions-v2/` (palette section, context/dropdown menu items, selection actions, toolbar, sidebar panel, settings, drop menu, forms/preview/job-progress/result) + `use-v2-palette` bridge and `V2LibraryDropZone`. Real-app additive wiring: palette `v2tool:` entries + dispatch, FileRowMenu `v2Items`, bulk-bar `v2Actions` slot, ExtensionsTab `v2Settings` slot, Library drop zone in `page.tsx`; v1 entries/shortcuts untouched | `contributions.test.ts` (10: ordering, collisions, disabled exclusion, capability denial, sanitization, fields, drop validation, catalog-regression) + `adapters.test.tsx` (13 jsdom interaction incl. context changes + disable cleanup) + `extensions-v2-ui.test.tsx` (7: real FileRowMenu/CommandPalette/ExtensionsTab/BulkBar + enable/disable + settings routes) + tsc clean + eslint 0 errors (3 pre-existing `entryId` warnings) | R6 row (this); authoring/API guide + UI diagrams deferred to #172/#173 |
| R7 settings, state, events | #169 | DONE: `packages/yard-core/src/extensions-v2/` (`extension-data.ts` validated settings store + versioned state store with transactional migrations and bounds; `events.ts` typed bus with disposal + gap sequences; `definition.ts` setting validation; `permissions.ts` approval snapshot/restore; `jobs.ts` newest-50 snapshot retention); app `src/lib/extensions-v2/` (`events.ts` bus, `settings-state.ts` settings-table rows with persist-before-notify, `policy.ts` approval persistence under `v2:approvals`, `jobs.ts` transition-only emission) | `extension-data/events` tests (12: isolation, migration-failure preservation, persist-before-notify, disposal) + approval round-trip + retention | settings/events guides deferred to #173 |
| R8 Make Pack v2 | #171 | DONE: `packages/yard-tools/make-pack-v2/` (definition with 3 commands/settings/contributions, pure `policy.ts`, `handlers.ts` on v2 services only; general engine gap fixed in core: `V2HandlerContext.runMode` direct/apply/job in `host.ts`, manifest forwarded as an in-memory `V2ArchiveEntry` in `operations.ts` + `index.ts` export); app `src/lib/extensions-v2/` (real stored-ZIP `archive.ts` codec, `sources.ts` shelf/recent adapters over persisted records, `make-pack-v2.ts` registration wired in `host.ts` with `selectionSources`, `grants` + `approvals` routes); UI `MakePackV2Dialog` + hook, `V2ExtensionsSection` with Approve, `V2ExtensionSettings` approve button, page prop-drilling (`resolveV2FileItems`/`onV2Command` through FileTable chain, bulk-bar `v2Actions`, `v2Settings` through SettingsDialog types/body/tab, shelf/bulk Pack-v2 entries); v1 packages/routes untouched; no v1 imports (package + app-glue specifier scans) | `policy.test.ts` (11) + `handlers.test.ts` (16: folder/ZIP x shelf/recent/selection, manifest on/off, collisions, missing, conflicts, B12 sidecar, cancel w/ owned-only cleanup, permissions, contributions) + `src/test/integration/make-pack-v2.test.ts` (17: real-fs exports, independent ZIP-reader integrity, interruption, limits, sources, registration/routes/boundary) + `MakePackV2Dialog.test.tsx` (3 jsdom) + core `host` runMode + `operations` manifest-entry tests; tsc clean | parity table + policies + enable steps in `packages/yard-tools/make-pack-v2/README.md`; walkthrough deferred to #173 |
| R9 authoring, diagnostics | #172 | DONE: `scripts/scaffold-extension-v2.cjs` (kebab-case-validated scaffold writing package.json + definition/handlers/index + vitest suite + README with the explicit static-registration snippet; bundled packages register statically, no installer); `src/app/prototype/ext-v2-workbench/` (`page.tsx` workbench previewing all 9 production contribution points, sanitized catalog, availability checks, direct/job invocation against the disposable dev Library with cancel, and sanitized host history; `inspector.tsx` bounded execution-inspector log surfacing invocation/job IDs, transitions, availability reasons, denied permission names, sanitized errors, and `runMode` with local-detail vs sanitized-export split; `redact.ts` ID-preserving redactor with 2000-char/100-record bounds; `fixture-handlers.ts` idempotent dev-only surface+worker handler registration; `reload/route.ts` explicit reload proving second reloads attach nothing; `history/route.ts` sanitized bounded job history); fixtures beyond Make Pack: `fixture-surface` (otherwise-unused points/scopes, pure, `runMode` echoes) and `fixture-worker` (cancellable count job, caught `files:write` denial probe, isolated `last-count` state), both registered through the production registry with no fixture-ID branches in production code; three-layer production exclusion (prototype `notFound` + route 404s + electron-builder prototype ignore) | scaffold output typechecks (scoped `tsc -p`) and runs (generated suite 3/3) + rejection cases; fixture execution incl. `runMode=direct/job` echoes, job progress, named denial, state isolation; reload idempotency + unregister disposal; production refusal; no-branch source scan; build-exclusion assertions; redactor/export bounds (`redact` 7 + `inspector` 2 + scaffold 4 + workbench 8) + tsc clean + eslint 0 errors | scaffold README is the runnable authoring reference; full authoring/API guide + troubleshooting deferred to #173 |
| R10 repo contracts | #173 | DONE: runtime discovery in `src/lib/runtime-info.ts` (v1+v2 `extensionSystems`, per-extension `extensionsV2` with declared/effective permissions + `approvalsKnown`, commands, contributions, settings schema, `eventsV2`; `readRuntimeDatabaseFlags` read-only helper replaces the read-write enabled overlay, fixing the init/migration side effect); 4 new guides (`docs/extensions-v2.md` authoring/API + 9 runnable author examples, `docs/extensions-v2-migration.md`, `docs/extensions-v2-make-pack.md`, `docs/extensions-v2-troubleshooting.md`) registered in `DOCUMENT_REGISTRY` + `prepare-docs.cjs` + `check-docs.cjs` with parity tests; 13 guides + README + RELEASE.md updated additively; `public/extension-system-v2.html` implementation companion (5 offline SVG diagrams) linked from the proposal; runnable `examples/extensions-v2/minimal/` (`bun run example:v2-minimal`) registered in the docs manifest; `scripts/check-v2-boundaries.cjs` in CI (`check.yml`) enforcing import direction + workspace inclusion + prototype exclusion | `extensions-v2-discovery.test.ts` (6: identity, events, no-handler-execution probe, no-secrets, read-only temp-DB proof, corrupt-row degrade) + `extension-v2-repo-contracts.test.ts` (6: new IDs, script parity, no prompt/audit registration, staged prepare+check round-trip, build contents, boundary script) + tsc clean | index + affected guides, release notes, migration guide |
| R11 verification | #174 | DONE (evidence pass 2026-09-06, uncommitted): v2 scope `bunx vitest run` over core/extensions-v2 + make-pack-v2 + v2 components + workbench + 6 v2 integration suites → 29 files, 289 passed. Full `bun run test` → 47 files, 389 passed + 12 expected fails (ledger holds; `check-expected-failures` OK). `bun run typecheck` clean; `bun run lint` 0 errors (3 pre-existing `entryId` warnings). Coverage 64.29% stmt / 54.59% branch / 59.57% func / 65.56% lines vs thresholds 36.5/31/34/37.5 (pass, none lowered). `docs:prepare` 26 staged + `docs:check` OK (18/18 command refs); `check-v2-boundaries` OK; `example:selected-ids` 3/3, `example:v2-minimal` 4/4, `query-library` assertions pass; `bun run build` clean (all v2 routes + workbench collected; production exclusion via notFound/404 + `!.next/server/app/prototype/**` in electron-builder.yml, asserted by repo-contracts test). Scaffold probe `r11-probe-ext`: generated 3/3 pass, probe removed. Desktop `release:build` BLOCKED by environment (clean-binary refused: better_sqlite3.node locked by running process; killing node.exe would end this session — recorded as external blocker, no packaged install claimed; dist-electron/ holds only stale 0.1.0 artifacts). RELEASE.md extended with enable/disable steps (was the one gap) | R11 evidence list, no lowered thresholds | limitations, handoff |

Areas with no change needed yet: v1 packages, routes, settings formats, and UI behavior (preserved by coexistence ADR; additive integration only).
