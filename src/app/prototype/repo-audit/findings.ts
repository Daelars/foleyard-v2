// Current-source audit, 2026-09-05. Performance items are hypotheses until measured.
export const findings = [
  { id: "E04", area: "Extensions", priority: "P1", title: "Drop Rules apply bypasses filesystem grants", evidence: "drop-rules.apply has no transport adapter, so raw file paths and targetDirectory pass through. Its service copies directly without calling the host filesystem checks.", proposal: "Hydrate source IDs and resolve every destination through host-owned file operations. Deny filesystem commands without a transport policy.", verify: "With Drop Rules enabled, a request outside Library roots or without a destination grant must fail before touching disk.", source: "packages/yard-tools/drop-rules/src/service.ts" },
  { id: "B12", area: "Bugs", priority: "P1", title: "Pack manifest temporary name can destroy an existing file", evidence: "ZIP creation writes .<packName>-manifest.tmp.json in the destination and unconditionally removes it in finally. The filename is predictable and not reserved exclusively.", proposal: "Use an exclusively created operation-owned temporary directory or stream the manifest entry. Reserve the final ZIP exclusively too.", verify: "A pre-existing manifest sidecar survives; concurrent same-name exports cannot share or delete each other's temporary files.", source: "packages/yard-tools/make-pack/src/service.ts" },
  { id: "B01", area: "Bugs", priority: "P1", title: "Gather can overwrite existing audio", evidence: "library-gatherer/src/service.ts: makeUniqueOutputPath checks only plannedNames; copyFile replaces an existing output. Different-size files bypass duplicate detection.", proposal: "Reserve names against disk and the current plan. Copy exclusively, stage output, then publish a report of actual completed files.", verify: "Existing hit.wav survives a gather of another hit.wav, including a destination created after preview.", source: "packages/yard-tools/library-gatherer/src/service.ts" },
  { id: "B02", area: "Indexing", priority: "P1", title: "Move detection can merge distinct recordings", evidence: "reconcileMovedFiles matches name, size and audio metadata, copies tags and collections, then deletes the old identity. No content identity or one-to-one reservation.", proposal: "Keep stable file IDs. Treat metadata matches as candidates; require stronger identity before relinking and preserve ambiguous records.", verify: "Two distinct same-size recordings never inherit each other's tags. Shelf and recent-pack references survive a real move.", source: "src/lib/database/files/batch.ts" },
  { id: "B03", area: "Bugs", priority: "P2", title: "Collection search ignores the query", evidence: "getFiles and getFileCount use a separate collection branch whose filters omit query and favorites.", proposal: "Use one filter builder for collection membership plus the other active filters.", verify: "A collection query with no matching filename returns zero rows and a zero count.", source: "src/lib/database/files/reads.ts" },
  { id: "B04", area: "Bugs", priority: "P1", title: "Rollback erases newer edits", evidence: "rollbackBulkTags restores the whole prior tag list. removeMany restores the entire old file list on failure, even after navigation.", proposal: "Associate requests with view generations and per-field mutation versions. Undo only the failed operation when it is still current.", verify: "Delayed failure cannot erase a later successful tag or replace files from a newly selected root.", source: "src/app/library/use-library-files.ts" },
  { id: "B05", area: "Indexing", priority: "P2", title: "New files may need a second scan for metadata", evidence: "fullParse is existingByPath.get(path)?.duration === null. A new record produces false, and the header parser does not fall back when false.", proposal: "Use header-first parsing with a full-parse fallback in the same job when required fields are missing.", verify: "A fixture whose metadata is beyond the first 32 KiB obtains duration on its first scan.", source: "src/lib/scanner/reconcile.ts" },
  { id: "B06", area: "Extensions", priority: "P2", title: "Folder Janitor silently truncates at 5,000 files", evidence: "resolveJanitorScanFolder requests a single page with MAX_SCAN_FOLDER_FILES and returns no truncation flag.", proposal: "Page through the scoped index or run a cancellable job; report completeness and inaccessible paths.", verify: "A folder containing 5,001 files either scans all of them or explicitly reports an incomplete result.", source: "src/app/api/extensions/execute/transport.ts" },
  { id: "B07", area: "Extensions", priority: "P2", title: "Scan folder inspects unrelated roots", evidence: "The folder transport passes every libraryRoot; FolderJanitorService.scan walks all roots to find empty directories.", proposal: "Carry an explicit scan scope through the command and constrain both file and directory work to it.", verify: "Scanning root A/sub never reports or traverses root B.", source: "packages/yard-tools/folder-janitor/src/service.ts" },
  { id: "B08", area: "Bugs", priority: "P2", title: "Malformed extension requests escape validation", evidence: "POST casts request.json to a type, then accesses fields before a try/catch. null and malformed JSON throw.", proposal: "Use readMutationBody, validate the entire transport envelope, and map transport failures consistently.", verify: "null, malformed JSON, wrong selection types and unknown commands receive controlled client errors.", source: "src/app/api/extensions/execute/route.ts" },
  { id: "B09", area: "Bugs", priority: "P2", title: "Smart collection conversion builds one huge INSERT", evidence: "convertToRegularCollection creates one values array for all matches and does not transact membership plus conversion.", proposal: "Use INSERT SELECT inside one transaction with the collection update.", verify: "Convert a large collection beyond SQLite's parameter limit; injected failure leaves the smart collection intact.", source: "src/lib/database/collection-repository.ts" },
  { id: "B10", area: "Indexing", priority: "P2", title: "Remove from library returns on the next scan", evidence: "Removal sets removedAt; reconciliation treats a rediscovered removed file as changed and clears removedAt.", proposal: "Distinguish user exclusions from missing files. Decide whether remove means hide-until-rescan or persistent exclusion, and name it accurately.", verify: "A user exclusion survives scans; a temporarily missing file can return normally.", source: "src/lib/files/delete-files.ts" },
  { id: "B11", area: "Bugs", priority: "P2", title: "Old pagination completion unlocks a newer request", evidence: "loadFiles resets the loading-more ref; any previous loadMore finally clears it without a request-generation check.", proposal: "Give each load-more request ownership of its lock; abort obsolete requests and deduplicate appended IDs.", verify: "A delayed old page cannot enable two simultaneous requests for the same new offset.", source: "src/app/library/use-library-files.ts" },
  { id: "E01", area: "Extensions", priority: "P1", title: "Permissions rely on extension cooperation", evidence: "createYardExtensionContext exposes options.services unchanged. Permissions come directly from the manifest; services do not enforce them.", proposal: "Enforce grants in host-owned operations. Keep bundled extensions trusted; isolate processes before allowing third-party executable packages.", verify: "An extension with no write grant cannot mark files removed, even if it omits permissions.require.", source: "packages/yard-core/src/extensions/extension-context.ts" },
  { id: "E02", area: "Extensions", priority: "P2", title: "Adding a tool still means editing the app", evidence: "Registry imports, transportAdapters, UI intents and dialog hooks each know individual extension IDs.", proposal: "Make each built-in definition own its manifest, validation, transport adapter and UI contribution. Keep one host execution path.", verify: "A small built-in extension registers without edits to the generic execute route or several app switches.", source: "src/lib/extensions/registry.ts" },
  { id: "E03", area: "Extensions", priority: "P2", title: "Long commands have no job lifecycle", evidence: "The execute route awaits a single result. Host progress support exists but the route supplies no callback or cancellation signal.", proposal: "Introduce job IDs, bounded progress events, cancellation, per-item outcomes and safe retry rules for long operations.", verify: "Close and reopen the tool while it runs; progress remains available and cancellation stops scheduling work.", source: "src/app/api/extensions/execute/route.ts" },
  { id: "I01", area: "Indexing", priority: "P2", title: "Metadata queue has no backpressure", evidence: "Eight workers limit active work, but enqueue can retain every remaining file; pending.shift also shifts array entries.", proposal: "Bound pending work and make discovery await capacity. Expose pending, active, failed and completed counts.", verify: "Slow metadata over 100,000 generated file records keeps pending work below a fixed cap.", source: "src/lib/scanner/metadata-queue.ts" },
  { id: "I02", area: "Indexing", priority: "P2", title: "Every scan loads and touches the whole index", evidence: "ScanRunner loads all active and removed rows, stores every seen path, and writes timestamps for unchanged files.", proposal: "Use scan generations and scoped SQL reconciliation; keep discovery status per root. Add incremental watching only after full-scan recovery is sound.", verify: "Measure unchanged-scan writes, peak memory and elapsed time at 10k and 100k records.", source: "src/lib/scanner/scan-runner.ts" },
  { id: "I03", area: "Indexing", priority: "P2", title: "Overlapping roots repeat work and change ownership", evidence: "Each root is walked independently; seenPaths is not used to skip indexing duplicates. Last root processed wins libraryRoot.", proposal: "Define longest-root ownership or reject nested roots. Canonicalize roots and deduplicate discovery.", verify: "Reordering a parent and child root leaves ownership, counts and metadata work unchanged.", source: "src/lib/scanner/discovery.ts" },
  { id: "P01", area: "Performance", priority: "P2", title: "Warm waveforms wait behind cold generation", evidence: "Cache reads happen inside the two-slot generation semaphore. Client rows refetch on remount with no-store.", proposal: "Read valid disk entries before taking a generation slot; retain a bounded client cache keyed by source version.", verify: "Warm-cache latency stays low while two slow files generate; measure cold, warm and scroll revisit cases.", source: "src/lib/waveform-cache.ts" },
  { id: "P02", area: "Performance", priority: "P2", title: "Folder navigation reads all directories in a root", evidence: "getSubdirectoriesForRoot loads distinct directories then computes immediate children in JavaScript each time.", proposal: "Query immediate children from a maintained directory table or indexed parent relation.", verify: "Compare query plans and navigation latency for a root with 50,000 directories.", source: "src/lib/database/browse-repository.ts" },
  { id: "P03", area: "Performance", priority: "P2", title: "Search and sort need workload-specific indexes", evidence: "Substring LIKE, OFFSET paging and duration sort operate with separate single-column indexes. Every file page recounts favorites.", proposal: "Measure EXPLAIN QUERY PLAN; add justified composite indexes and cursor paging. Evaluate substring indexing without changing search semantics.", verify: "Record p50/p95 search latency and deep-page latency on representative libraries before and after.", source: "src/lib/database/files/reads.ts" },
  { id: "P04", area: "Performance", priority: "P3", title: "Shelf and pack hydration use one query per ID", evidence: "hydrateFiles and shapeShelfList call getFileById for every selected file; sources also repeatedly canonicalize roots.", proposal: "Batch lookup by IDs and resolve root paths once per operation, retaining final path checks.", verify: "1,000-item requests use a bounded number of queries and preserve requested order.", source: "src/app/api/extensions/execute/transport.ts" },
  { id: "S01", area: "Cleanup", priority: "P3", title: "Old toggle helpers are kept alive only by tests", evidence: "applyFavoriteToggle and applyTagToggle have no production call sites. Their tests exercise the superseded path.", proposal: "Delete the unused helpers and their tests together. Keep the batch behavior and add concurrent failure coverage.", verify: "Repository reference search has no live callers; typecheck and the remaining suite pass.", source: "src/app/library/file-query.ts" },
  { id: "S02", area: "Cleanup", priority: "P3", title: "Repository forwarding layers multiply navigation", evidence: "db barrel forwards to singleton functions, which forward to class methods, which forward to reads/writes with repeated context objects.", proposal: "Keep a single repository instance and one adapter boundary. Replace pageLimit() with a constant; remove identity transport lambdas.", verify: "Public behavior stays covered by repository and route tests; callers cross fewer files.", source: "src/lib/database/file-repository.ts" },
  { id: "S03", area: "Cleanup", priority: "P3", title: "Source-text checks cannot prove UI behavior", evidence: "root-height-chain.test.ts forbids text tokens but never measures layout. Theme token checks are also structural.", proposal: "Retain cheap intentional policy checks; replace layout claims with browser smoke coverage at multiple zoom levels. Do not delete behavioral tests for being small.", verify: "The actual window remains filled and scrollable at 75%, 100% and 125% zoom.", source: "src/app/root-height-chain.test.ts" },
  { id: "S04", area: "Cleanup", priority: "P2", title: "CI omits the required production build", evidence: "check.yml runs TypeScript, ESLint and Vitest but no next build, despite the repo's shipping rule.", proposal: "Add the production build to PR checks, keep prototype code on throwaway branches, and update stale audit status documents after implementation.", verify: "A PR with a build-only regression cannot pass CI.", source: ".github/workflows/check.yml" },
  { id: "T01", area: "Tests", priority: "P2", title: "Passing tests measured nothing about the product", evidence: "410 tests passed with no coverage provider installed at all. The number counted assertions grouped into it blocks, not exercised code, so the suite's size read as health.", proposal: "Install a coverage provider, publish the measured baseline, and threshold it so a drop fails the run. Rebuild the suite around integration tests that load the defective modules.", verify: "bun run test:coverage reports line, branch, function and statement coverage with thresholds at the measured baseline.", source: "docs/test-coverage-baseline.md" },
  { id: "T02", area: "Tests", priority: "P2", title: "A third of the product loaded in no test", evidence: "Static import analysis: of 293 production modules, only 96 (32.8%) are directly imported by any test file, and 96 of 225 reported files are wholly unexecuted. The largest unexecuted file is use-library-files.ts at 252 statements, which holds B04 and B11.", proposal: "Drive the rebuild through the real entrypoints (routes, scan, host, hooks) so the defective modules load. Record the target shape with per-area test budgets.", verify: "The coverage report enumerates wholly-unexecuted files instead of inferring them, and the count falls batch to batch.", source: "docs/test-coverage-baseline.md" },
  { id: "T03", area: "Tests", priority: "P3", title: "Some tests could only pass by not existing", evidence: "The Vitest include list named three trees, so electron/ and scripts/ had nowhere to put a collected test; only *.test.ts matched, so *.test.tsx would vanish. The Electron main-process hardening test had to be parked in src/lib to run at all.", proposal: "Collect by pattern across the repo and add a guard test that walks the disk and fails when any test file sits outside the include globs.", verify: "A probe test file in an uncovered directory fails the guard, and covering it passes.", source: "vitest.config.ts" },
  { id: "T04", area: "Tests", priority: "P3", title: "Unit tests crowded a third of the modules", evidence: "410 tests crowded onto a third of the modules at 1.9 assertions each while the rest had none. Roughly half of the extension-area tests asserted wiring rather than behaviour, and three host behaviours were re-tested once per extension.", proposal: "Rebuild from 410 mostly-unit tests to roughly 50 integration tests across eight areas, asserting shared behaviour once and stating the correct contract as expected-to-fail where defects live.", verify: "Every file is assigned to exactly one area; each area ends lighter with coverage at or above baseline.", source: "docs/test-suite-target-shape.md" },
] as const;
